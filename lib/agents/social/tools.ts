import { tool } from 'ai'
import { z } from 'zod'

/**
 * Tools for social content agent.
 * Add more tools (e.g. web search, trend lookup) as needed.
 */
export const socialTools = {
  // Placeholder – expand later
} as const
